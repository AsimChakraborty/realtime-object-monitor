"""Application packages (core, database, service, utils).

Entrypoints (`app.py`, `dashboard.py`) live at the project root and
bootstrap this directory onto ``sys.path`` so the packages here are importable.
"""